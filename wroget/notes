
starts with #    -> comment

between <-- and -->    -> comments? original page number

between begin and end /^%$/ -> neo section header (fairly useless)

starts with \s+#
	after #, \d+ is the section number
	then a dot
	then maybe a section name comment, inside []
	then the section name
	then a dot and two dashes
  then it becomes a new subsection

subsection
	begins in the above whatever or on a new line with 5 spaces indenting
	may begin with "entry type": (Adv|Adj|N|V|Phr|??) followed by dot
	divided into groups by ;
	groups divided into entries by ,
	commas can be are quoted
	entries may end in a cross reference

